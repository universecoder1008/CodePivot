{
  "name": "placement-preparation",
  "version": "1.0.0",
  "scripts": {
    "frontend": "npm run dev --prefix frontend-main",
    "backend": "npm start --prefix backend",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}